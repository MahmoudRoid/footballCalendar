<?xml version="1.0" encoding="utf-8"?>
<mahmoud.footballcalendar.Classes.LockedHorizontalScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:scrollbars="none"
    android:background="@android:color/black">
    <!-- Used scroll view to handle large images (android won't show images correctly without this) -->

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent">

        <!-- ImageViews to show images,,, we play animation sets on these to flip.... !-->
        <ImageView
            android:id="@+id/aif_img1"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop" />

        <ImageView
            android:id="@+id/aif_img2"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop" />
    </RelativeLayout>

</mahmoud.footballcalendar.Classes.LockedHorizontalScrollView>